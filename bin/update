# -*- shell-script -*-

# Change to the project root directory.
PROJECT_HOME="$(cd $(dirname $BASH_SOURCE)/..; pwd)"
cd $PROJECT_HOME

# Source bootstrap utilities.
# source "$(dirname $BASH_SOURCE)/../opt/bootstrap/bootstrap.bash"

#echo Getting the latest load balancer configuration file from git.
#(cd etc;
# git archive --remote=git@git:lb master:etc LBAdmin.yaml | tar -x)

# If there is an update, process it.
[[ -n "$(git status --porcelain etc/LBAdmin.yaml)" ]] || {
    echo No changes
    exit
}

echo Reloading configuration.
# TODO: systemctl reload riemann-ping

# TODO: git commit 
